Build the docker file and run it:
1. cd to folder flaskApp
2.Build the  docker image : sudo docker build -t image-name:latest .
3.run : sudo docker run -d -p 5000:5000 image-name
4.open the app in web browser :  http://localhost:5000/


Run without docker:
1.cd to flaskApp folder 
2.run : python app.py
3.open the app in web browser :http://localhost:5000/
